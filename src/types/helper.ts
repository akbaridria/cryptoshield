export interface Menus {
  title: string;
  description: string;
  icon: JSX.Element
}

export interface IBlockies {
  address: string;
  customClass: string;
}

export interface IAccountSubMenu {
  name: string;
  icon: JSX.Element
}